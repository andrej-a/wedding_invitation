import React from 'react';
import PreviewOverlay from '../PreviewOverlay';

type Props = {};

const App = (props: Props) => {
    return (
        <>
            <PreviewOverlay />
        </>
    );
};

export default App;
